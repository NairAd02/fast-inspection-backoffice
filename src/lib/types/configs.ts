export interface Config {
  version: number;
  nombre: string;
  descripcion: string;
  state: boolean;
  porcentajeCompletitud: number;
}

export interface ConfigDetails {
  version: number;
  nombre: string;
  descripcion: string;
  state: boolean;
  porcentajeCompletitud: number;
}
